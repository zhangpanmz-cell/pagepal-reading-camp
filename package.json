{
  "name": "pagepal-reading-camp",
  "description": "页伴 Pagepal：围绕一本书的七天读写本地网页",
  "version": "0.1.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node server.mjs",
    "start": "npm run build && node server.mjs",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "19.3.0",
    "weread-agent-cli": "0.1.4"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
