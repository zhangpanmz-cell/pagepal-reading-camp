# Copy to server/.env.deepseek.local, then enter a DeepSeek key in a local editor.
# Never send the key in chat or add it to browser localStorage.
# The old OpenAI configuration is not read or reused.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-flash
